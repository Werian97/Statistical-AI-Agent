[project]
name = "statistical-ai-agent"
version = "1.0.0"
description = "An AI agent for statistical analysis of CSV datasets, using Python for orchestration and a C shared library for numerical computation."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
